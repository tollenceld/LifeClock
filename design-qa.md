# 刻度设计与质量基线

本文记录 LifeClock 首次进入版本管理时的实现状态。它是一份可复核的工程基线，不替代发布前的真机验证和 App Store 审核。

## 设计原则

- 保持安静的石墨色界面、等宽数字和克制的琥珀色强调。
- 关键标题、数值和标签不依赖玻璃效果承载可读性。
- 人生、年、月、周、日共用稳定的指标、仪表、事件和导航基线。
- 连续拖动用于理解尺度关系，离散按钮用于准确选择。
- 减少动态效果开启时，取消空间形变和弹簧位移。

## 五种尺度

- **人生**：以年龄为语义单元，显示当前十年、年龄阶段和下一次生日。
- **年**：以真实日历日为单元，兼容 365 天与 366 天。
- **月**：使用 42 个日历槽位，并服从用户选择的周起始日。
- **周**：将七天拆分为凌晨、上午、下午和夜间四个时段。
- **日**：使用真实的 23、24 或 25 小时，兼容夏令时切换。

## 交互与辅助功能

- 五个尺度按钮均提供稳定的辅助功能标识与中文 VoiceOver 标签。
- 各尺度提供主仪表、阶段信息和尺度关系摘要。
- 主要触控目标不小于 44pt。
- 支持动态字体、深浅色外观和减少动态效果。
- 事件创建、编辑、删除与设置流程包含稳定的 UI 自动化入口。

## 自动化验证

首次建仓基线在 iPhone 17 Pro、iOS 26.3 模拟器完成验证：

- Debug 构建与启动：通过
- Swift Testing 核心测试：17 通过，0 失败
- XCTest UI 测试：6 通过，0 失败
- 首次引导、设置、事件增删改：通过
- 五尺度点击与拖动切换：通过

## 发布前验证

正式发布前仍需完成签名、正式 Bundle Identifier、无调试器真机冷启动、持久化检查以及 Archive 验证。详细事项见 [`StoreAssets/release-checklist.md`](StoreAssets/release-checklist.md)。
