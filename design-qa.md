# 刻度设计与质量基线

本文记录 LifeClock 的当前设计约定与历次验证结果，不替代发布前的真机验证和 App Store 审核。

## 当前设计原则

- 主屏只呈现时间数字、可视化主图和底部导航；默认进入人生。
- 四尺度采用统一的小数天数与细刻度；暖琥珀色累计、冷青色倒数，天数为视觉中心。
- 计时器直接置于背景，不使用大面积卡片；关键标题、数值和标签不依赖玻璃效果承载可读性。
- 人生、日、月、年四尺度共用底部导航；刻点管理从设置进入。
- 日月年胶囊支持点击和拖动，主图左右轻扫切换尺度；人生轻点读数翻面，也可点击相连的「生之时／死之时」状态直接选择。
- 减少动态效果开启时，以淡入淡出替代三维翻转，并取消空间形变和弹簧位移。

## 四种尺度

- **人生**：只显示累计／剩余天数，保留 8 位小数。小时、分钟和秒并入小数天数；年龄与百分比不再重复显示。暖、冷双面状态以相向弧线和细下划线相连。
- **日**：今日已度过的小数天数，按真实秒数换算，兼容 23、24 或 25 小时的夏令时日，保留每日刻点。
- **月**：本月已度过的小数天数，去掉月历大卡片，以细周期刻度保留月度刻点。
- **年**：今年已度过的小数天数，兼容 365／366 天，去掉年度点阵，以细周期刻度保留年度刻点。

周尺度、周起始设置入口和新建每周刻点选项已移除。已有每周记录保留原始规则和日期，在刻点集的「历史刻点」中查看、删除或主动转换，不自动改写，也不再作为活动刻点运行。

## 交互、性能与辅助功能

- 底部由人生入口和日月年／设置一体胶囊组成。人生页展开约 96pt，其他尺度收为 58pt 圆钮，右侧同步伸缩；触控目标不小于 44pt。邻近刻点使用菜单避免触控目标重叠。
- iOS 26 使用原生 `GlassEffectContainer` 与交互玻璃；iOS 18–25 使用材质降级。
- 设置以弹层打开，关闭后回到原尺度；同一次使用期间保留人生翻面状态，重启回到正面。
- 当前尺度可见且应用在前台时每 0.1 秒取实际时间；被弹层遮挡、切换尺度或进入后台时暂停，恢复时跳到实际时间。
- Canvas 刻度最高 30fps，仅绘制附近真实秒刻度，不重新计算日历或查询刻点。数字每 0.1 秒更新，日历快照单独按分钟更新。减少动态效果时刻度静止，数字继续更新。
- 支持动态字体、浅深外观和 VoiceOver；提供完整数值、双面名称与选中状态，不逐秒自动播报。
- 1 天固定为 86400 秒，周期起止仍遵守日历与时区。核心测试负责数值、边界及互补关系；UI 测试负责操作路径、数字变化、翻面、导航和旧规则转换。

## 2026-09-15 小数天数与四尺度统一验收

本轮将人生合并为小数天数，统一日月年读数与刻度，并调整导航顺序。验收包括真实数字增减、跨日与夏令时、到期状态、双面切换、导航顺序、设置返回、刻点兼容、紧凑屏与大字号。

本次执行结果、截图和录屏集中记录在 [改造记录](DesignReview/README.md)。前一版原子时钟的 43 项通过记录属于历史基线，不替代本轮验证。

## 历史自动化验证

以下仅描述当时版本，含已移除的周尺度和旧主屏结构。

首次建仓基线在 iPhone 17 Pro、iOS 26.3 模拟器完成验证：Debug 构建与启动通过，Swift Testing 核心测试 17 通过、0 失败，XCTest UI 测试 6 通过、0 失败；首次引导、设置、事件增删改、当时五尺度点击与拖动切换通过。

2026-09-08 新增暖纸/石墨外观选择、按月份组织的年度点阵、刻点集和原生生日选择，并修复星期标签、自然日相对时间和保存失败回滚。随后累计 20 条核心与布局测试和 9 条 UI 测试通过。原始截图与执行细节保留在 [历史改造记录](DesignReview/README.md)。

## 发布前验证

正式发布前仍需完成签名、正式 Bundle Identifier、无调试器真机冷启动、持久化检查以及 Archive 验证。详细事项见 [`StoreAssets/release-checklist.md`](StoreAssets/release-checklist.md)。本轮保留 iOS 18 最低版本和离线存储，不涉及发布、签名或商店素材重制。
